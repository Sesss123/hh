# Splash asset placeholder (non-binary)

Do not commit binary image files in this repository.

At app integration time, add your production hero image locally as:

- `assets/splash/island.png`

The splash screen already includes a safe fallback gradient if this file is missing.
